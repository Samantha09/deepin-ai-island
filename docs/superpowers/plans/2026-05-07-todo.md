# 待办事项清单（2026-05-07）

## 一、当前进行中的工作

### 1. 子 Agent 识别与显示功能（核心发现）

**状态**：代码已写好但未实际写入磁盘（缓存不同步问题）

**关键发现**（已完成排查）：
- 子 Agent 消息**不在主 transcript 中**，而是存储在独立目录：
  - `~/.claude/projects/<project>/<sessionId>/subagents/<agentId>.meta.json`
  - `~/.claude/projects/<project>/<sessionId>/subagents/<agentId>.jsonl`
- meta.json 内容示例：`{"agentType": "general-purpose", "description": "..."}`
- jsonl 中 `isSidechain: true` 是子 Agent 消息的标记
- tasks 目录也有记录：`~/.claude/tasks/<sessionId>/<taskId>.json`

**已写但未落地的代码**：
1. `island_ui/session.py`：Session 类增加 `subagents: list[dict]` 字段
2. `island_ui/island_window.py`：
   - 增加 `_scan_subagents(session_id)` 静态方法，扫描 subagents 目录
   - `_push_sessions_to_web` 中附加 subagents 数据
3. `island_ui/web/island.html`：
   - CSS 增加 `.session-subagents` 和 `.subagent-tag` 样式
   - JS 在会话卡片中渲染子 Agent 标签列表

**状态**：已完成 ✅（参考 open-vibe-island 重新实现）

**实现内容**：
- 新增 `SubagentStarted`/`SubagentStopped` 事件类型
- `claude_code_source.py` 解析 `SubagentStart`/`SubagentStop` hook 事件
- `Session` 类通过 `add_subagent`/`remove_subagent`/`active_subagents` 维护子 Agent 列表
- `island_window.py` 处理子 Agent 生命周期，会话结束时自动清空
- 前端显示子 Agent 类型 + 运行时间（最多一行）
- 在 `~/.claude/settings.json` 注册了 `SubagentStart`/`SubagentStop` hook
- 移除了文件扫描逻辑（`_scan_subagents`）
- 提交: `88bf300`

---

## 二、之前 Pending 的任务

### 2. 音效功能与状态修复（Task #14）

**状态**：核心功能已完成，部分验证 pending

**已完成**：
- SoundPlugin 实现（begin/alarm/end 音效）
- 全局音量控制 + 静音开关
- 配置持久化（default.yaml + ConfigManager）
- 清除已完成会话按钮 + 设置下拉菜单
- 修复 completed 状态被覆盖为 idle 的问题
- 12 个单元测试通过

**待验证**：
- [ ] Task #19: Mock 模式集成验证（清除按钮、设置菜单交互）
- [ ] 实际使用中的音效触发时机验证

### 3. Subagent-Driven Development Demo（Task #20, #21）

**状态**：Task 1 已完成（文档字符串），Task 2 pending

**说明**：这是用户要求演示 skill 流程的测试任务，优先级低。可在主功能完成后顺手做完。

---

## 三、已知问题

1. **文件缓存不同步**：Edit 工具修改后，Read 缓存显示有变化，但 `cat`/`git diff` 显示无变化。明天需用 Write 工具强制覆盖。
2. **Worktree 环境**：当前在 `worktree-subagent-demo` 分支，修改完成后需决定是合并回 main 还是废弃。

---

## 四、明日行动顺序建议

1. 修复文件写入问题（用 Write 重写三份文件）
2. 提交子 Agent 功能代码
3. 启动 AI Island 验证
4. 补完音效功能的 Mock 验证（Task #19）
5. 决定是否完成 Subagent Demo 的 Task 2（类型注解）
6. 如主功能全部完成，用 `superpowers:finishing-a-development-branch` 收尾
